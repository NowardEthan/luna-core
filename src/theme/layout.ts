/** Insets horizontais padronizados — conteúdo scroll vs barra do composer. */
export const layout = {
  /** Colunas de texto, listas, hero (Home, Ajustes, etc.). */
  screenPaddingX: 20,
  /** Composer — mais “full”, colado às bordas com respiro mínimo. */
  composerPaddingX: 12,
} as const;
