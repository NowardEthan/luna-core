/**
 * Feature flags do app. Desligar aqui ESCONDE o acesso à feature sem apagar o
 * código — basta voltar `true` para reativar.
 */

/** Terço/Rosário. Desativado a pedido; o código (useRosary, sheets, HUD) fica intacto. */
export const ROSARY_ENABLED = false;
